eureka:
  client:
    enabled: false
spring:
  application:
    name: hello-world
  main:
    banner-mode: "off"
server:
  port: 8000
  compression:
    enabled: true
    mime-types: "application/json,application/xml,application/javascript,text/html,text/xml,text/plain"